// AI generated test

/*
    multi line comment
*/

module test;
import std;

namespace math {
    extern int pow(int a, int b);
    
    int add(int a, int b) {
        return a + b;
    }
    
    int complex_calc(int x, int y) {
        int result = (x * y) + (x / y) - (x % y);
        result = result & (result | 15);
        result = result ^ 255;
        result = ~result;
        return result;
    }
}

namespace utils {
    void print_numbers() {
        int i = 0;
        i++;
        ++i;
        i--;
        --i;
        i += 5;
        i -= 2;
        i *= 3;
        i /= 2;
        i %= 4;
        i &= 15;
        i |= 8;
        i ^= 3;
        i <<= 1;
        i >>= 1;
    }
}

const int GLOBAL_CONST = 42;

int main() {
    // Базовые выражения
    std::println("Testing parser...");
    
    // Арифметика
    int a = 10;
    int b = 20;
    int c = a + b;
    int d = a - b;
    int e = a * b;
    int f = b / a;
    int g = b % a;
    
    // Логические операции
    bool flag1 = true;
    bool flag2 = false;
    bool result1 = flag1 && flag2;
    bool result2 = flag1 || flag2;
    bool result3 = !flag1;
    
    // Побитовые операции
    int x = 12;
    int y = 10;
    int bit_and = x & y;
    int bit_or = x | y;
    int bit_xor = x ^ y;
    int bit_not = ~x;
    int shift_left = x << 2;
    int shift_right = x >> 1;
    
    // Сравнения
    bool cmp1 = a == b;
    bool cmp2 = a != b;
    bool cmp3 = a < b;
    bool cmp4 = a <= b;
    bool cmp5 = a > b;
    bool cmp6 = a >= b;
    
    // Вызовы функций
    int sum = math::add(5, 7);
    int power = math::pow(2, 8);
    
    // Составные присваивания
    int counter = 0;
    counter += 5;
    counter -= 2;
    counter *= 3;
    counter /= 2;
    counter %= 10;
    counter &= 15;
    counter |= 8;
    counter ^= 3;
    counter <<= 1;
    counter >>= 1;
    
    // Инкременты/декременты
    int idx = 0;
    idx++;
    ++idx;
    idx--;
    --idx;
    
    // Приоритет операторов
    int complex = a + b * c - d / e % f;
    int with_parens = (a + b) * (c - d);
    int mixed = a * b + c / d - e % f & g | h;
    
    // Возврат значения
    return 0;
}

// Еще один модуль для проверки импортов
namespace extra {
    const int VALUE = 100;
    
    void test_fn() {
        int x = VALUE;
        x = x + GLOBAL_CONST;
    }
}