module main;
import std;

// one line comment

/*
    multi
    line
    comment
    block
*/

int main() {
    std::println("Hello, World!");
    if(a == 0) {
        std::println("IfStmt is work");
    } 
    else std::println("ElseStmt is also work");

    for(int i = 0; i < n; ++i) {
        std::println("i={}", i);
    }
    
}
